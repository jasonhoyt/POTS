<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  	<%= f.select :product_id, Product.all.collect {|p| [ p.description, p.id ]} %>
  </div>
  <div class="field">
  	<%= f.select :vendor_id, Vendor.all.collect {|p| [ p.company_name, p.id ]} %>
  </div>
  <div class="field">
    <%= f.label :vpart_number %><br />
    <%= f.text_field :vpart_number %>
  </div>
  <div class="field">
    <%= f.label :quote_price %><br />
    $<%= f.text_field :quote_price %>
  </div>
  <div class="field">
    <%= f.label :units %><br />
    <%= f.text_field :units %>
  </div>
  <div class="field">
    <%= f.label :quote_info %><br />
    <%= f.text_field :quote_info %>
  </div>
  <div class="field">
    <%= f.label :expire_date %><br />
    <%= f.date_select :expire_date %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
